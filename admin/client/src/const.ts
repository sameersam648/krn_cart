export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// Placeholder login URL for development - replace with your own auth later
export const getLoginUrl = () => {
  return "/"; // Just redirect to home
};
